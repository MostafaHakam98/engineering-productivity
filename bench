#!/usr/bin/env bash
# Thin wrapper — delegates to ai/claude/benchmark/bench.sh
# Usage: ./bench "task prompt" [skill ...]
#        ./bench ai/claude/benchmark/tasks/T1.txt [skill ...]
SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
exec "$SCRIPT_DIR/ai/claude/benchmark/bench.sh" "$@"